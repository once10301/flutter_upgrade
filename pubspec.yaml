name: flutter_upgrade
description: flutter_upgrade
version: 0.0.2
homepage: https://github.com/once10301/flutter_upgrade

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.once10301.flutter_upgrade
        pluginClass: FlutterUpgradePlugin
      ios:
        pluginClass: FlutterUpgradePlugin